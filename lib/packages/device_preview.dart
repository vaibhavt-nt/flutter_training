import 'package:flutter/material.dart';

class DevicePreviewPackage extends StatelessWidget {
  const DevicePreviewPackage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('DevicePreview'),),
    );
  }
}
